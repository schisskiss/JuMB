<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent">
    
    
    
    <!-- Bearbeiten: Aussehen der Überschriften der Beiträge -->
    <TextView
        android:id="@+id/titleItem"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:paddingLeft="3dp"
        android:paddingRight="3dp"
        android:paddingBottom="10dp"
        android:textColor="#000000"
        android:textSize="25dp" />
 
    <!-- Bearbeiten: Aussehen des Datums der Beiträge -->	
    <TextView
        android:id="@+id/dateItem"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/titleItem"
        android:paddingLeft="7dp"
        android:paddingRight="7dp"
        android:textColor="#000000"
        android:textSize="20dp" />
    
    <!-- Bearbeiten: Aussehen des Beitrags -->
    <TextView
        android:id="@+id/descItem"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/dateItem"
        android:paddingLeft="7dp"
        android:paddingRight="7dp"
        android:textColor="#000000"
        android:textSize="20dp"
        android:maxLines = "10"/>
    
</RelativeLayout>